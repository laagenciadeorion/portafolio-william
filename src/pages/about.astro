---

import Hero from '../components/Hero.astro';
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Acerca de mi| William Monsivais" description="Acerca De William ">
	<div class="stack gap-20">
		<main class="wrapper about">
			<Hero
				title="Acerca de mi "
				tagline="Me dedico a forjar sueños digitales en universos creativos "
			>
				<img
					width="1553"
					height="873"
					src="/assets/acercademi.png"
					alt="Jeanine White at work with a colleague"
				/>
			</Hero>

			<section class="about-section">
  <h2 class="section-title gradient-text">Desarrollador Web & Diseñador Multimedia</h2>
  <div class="content flow">
    <p class="main-headline text-balance">
      <span class="highlight-word">De los píxeles al código:</span><br/>
      <span class="sub-headline">Creando experiencias digitales con estrategia y creatividad</span>
    </p>
    
    <div class="process-grid">
      <!-- Paso 1 -->
      <div class="process-card">
        <div class="process-header">
          <div class="process-number-wrapper">
            <span class="process-number">01</span>
          </div>
          <h3 class="process-title">Diseño Multimedia</h3>
        </div>
        <div class="process-content">
          <ul class="skill-list">
            <li><span class="icon">🎨</span> <strong>Diseño gráfico</strong>: Photoshop, Illustrator, Canva</li>
            <li><span class="icon">🎬</span> <strong>Edición audiovisual</strong>: CapCut, Adobe Premiere</li>
          </ul>
          <blockquote class="process-quote">
            "El buen diseño no solo atrae, sino que guía al usuario."
          </blockquote>
        </div>
      </div>

      <!-- Paso 2 -->
      <div class="process-card">
        <div class="process-header">
          <div class="process-number-wrapper">
            <span class="process-number">02</span>
          </div>
          <h3 class="process-title">Desarrollo WordPress</h3>
        </div>
        <div class="process-content">
          <ul class="skill-list">
            <li><span class="icon">🚀</span> <strong>WordPress</strong>: Divi y Elementor</li>
            <li><span class="icon">🛠️</span> <strong>Customización</strong>: Plugins, temas hijos, SEO</li>
          </ul>
          <blockquote class="process-quote">
            "Transformo plantillas en soluciones únicas que escalan."
          </blockquote>
        </div>
      </div>

      <!-- Paso 3 -->
      <div class="process-card">
        <div class="process-header">
          <div class="process-number-wrapper">
            <span class="process-number">03</span>
          </div>
          <h3 class="process-title">Desarrollo a Medida</h3>
        </div>
        <div class="process-content">
          <ul class="skill-list">
            <li><span class="icon">💻</span> <strong>Tecnologías</strong>: JavaScript, CSS, Astro</li>
            <li><span class="icon">🛒</span> <strong>E-commerce</strong>: Shopify</li>
          </ul>
          <blockquote class="process-quote">
            "Lo simple detrás de lo complejo: código eficiente con propósito."
          </blockquote>
        </div>
      </div>

      <!-- Paso 4 - Nuevo -->
      <div class="process-card">
        <div class="process-header">
          <div class="process-number-wrapper">
            <span class="process-number">04</span>
          </div>
          <h3 class="process-title">Actualizate</h3>
        </div>
        <div class="process-content">
          <ul class="skill-list">
            <li><span class="icon">⚡</span> <strong>Velocidad</strong>: Lighthouse, Web Vitals</li>
            <li><span class="icon">🔍</span> <strong>SEO Técnico</strong>: Estructura semántica</li>
          </ul>
          <blockquote class="process-quote">
            "Un sitio rápido no es un lujo, es la primera impresión de tu marca."
          </blockquote>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Educación -->
<section class="about-section">
  <h2 class="section-title">Educación & Habilidades Técnicas</h2>
  <div class="content">
    <div class="education-grid">
      <!-- Formación académica -->
      <div class="education-card">
        <div class="education-icon-wrapper">
          <span class="education-icon">🎓</span>
        </div>
        <div>
          <p class="education-title"><strong>Formación Académica</strong></p>
          <ul class="education-details">
            <li>Preparatoria Técnica en Analista Programador</li>
            <li>Carrera trunca en Mercadotecnia Digital</li>
            <li><em>Autodidacta en desarrollo web y diseño</em></li>
          </ul>
        </div>
      </div>

      <!-- Cursos y certificaciones -->
      <div class="education-card">
        <div class="education-icon-wrapper">
          <span class="education-icon">📜</span>
        </div>
        <div>
          <p class="education-title"><strong>Certificaciones</strong></p>
          <ul class="education-details">
            <li>WordPress Avanzado (Divi/Elementor)</li>
            <li>Google SEO & Marketing Digital</li>
            <li>Metodologías Ágiles (Scrum/Kanban)</li>
          </ul>
        </div>
      </div>

      <!-- Habilidades técnicas -->
      <div class="education-card">
        <div class="education-icon-wrapper">
          <span class="education-icon">💻</span>
        </div>
        <div>
          <p class="education-title"><strong>Dominio de Software</strong></p>
          <ul class="education-details skill-levels">
            <li>
              <span class="skill-name">Photoshop:</span>
              <span class="skill-bar" style="--width: 70%"></span>
              <span class="skill-percent">70%</span>
            </li>
            <li>
              <span class="skill-name">Illustrator:</span>
              <span class="skill-bar" style="--width: 50%"></span>
              <span class="skill-percent">50%</span>
            </li>
            <li>
              <span class="skill-name">Canva:</span>
              <span class="skill-bar" style="--width: 100%"></span>
              <span class="skill-percent">100%</span>
            </li>
            <li>
              <span class="skill-name">CapCut:</span>
              <span class="skill-bar" style="--width: 100%"></span>
              <span class="skill-percent">100%</span>
            </li>
            <li>
              <span class="skill-name">DaVinci Resolve:</span>
              <span class="skill-bar" style="--width: 100%"></span>
              <span class="skill-percent">100%</span>
            </li>
            <li>
              <span class="skill-name">Microsoft Office:</span>
              <span class="skill-bar" style="--width: 90%"></span>
              <span class="skill-percent">90%</span>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Habilidades -->

    
    <div class="cta-box">
      <p class="cta-text">
        ¿Listo para llevar tu proyecto al siguiente nivel?<br/>
        <strong>Desde el concepto visual hasta la implementación técnica.</strong>
      </p>
      
    </div>
  </div>
</section>

<style>
  /* Estilos base */
  .about-section {
  margin-top: 3rem; /* Ajusta este valor según necesites */
  padding-top: 2rem; /* Espacio interno superior */
  position: relative;
}

  .section-title {
    font-size: var(--text-2xl);
    color: var(--gray-0);
    margin-bottom: 1.5rem;
    position: relative;
    display: inline-block;
  }

  .section-title::after {
    content: '';
    display: block;
    width: 60px;
    height: 4px;
    background: var(--accent-regular);
    margin-top: 0.5rem;
    border-radius: 2px;
  }

  .gradient-text {
    background: linear-gradient(90deg, var(--accent-regular), var(--accent-light));
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }

  /* Contenido principal */
  .main-headline {
    font-size: clamp(2rem, 5vw, 3.5rem);
    line-height: 1.2;
    margin-bottom: 2rem;
    font-weight: 800;
    color: var(--gray-0);
  }

  .sub-headline {
    font-size: clamp(1.25rem, 3vw, 1.75rem);
    font-weight: 400;
    color: var(--gray-300);
    display: block;
    margin-top: 0.5rem;
  }

  .highlight-word {
    position: relative;
    z-index: 1;
  }

  .highlight-word::before {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 30%;
    background: var(--accent-light);
    opacity: 0.3;
    z-index: -1;
    border-radius: 4px;
  }

  /* GRID MEJORADO CON ANIMACIONES */
.process-grid {
  display: grid;
  gap: 1.5rem;
  margin-top: 2rem;
}

/* Mobile: 1 columna */
@media (max-width: 767px) {
  .process-grid {
    grid-template-columns: 1fr;
  }
}

/* Tablet: 2 columnas */
@media (min-width: 768px) and (max-width: 1199px) {
  .process-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

/* Desktop: 3 columnas con más espacio */
@media (min-width: 1200px) {
  .process-grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }
  
  /* Destacar la tarjeta central en desktop */
  .process-card:nth-child(2n) {
    transform: scale(1.02);
    box-shadow: 0 10px 30px rgba(var(--accent-rgb), 0.15);
  }
}

/* ANIMACIONES Y EFECTOS MEJORADOS */
.process-card {
  background: linear-gradient(145deg, var(--gray-800), var(--gray-850));
  border-radius: 16px;
  padding: 2rem;
  border-left: 5px solid var(--accent-regular);
  transition: all 0.4s cubic-bezier(0.22, 1, 0.36, 1);
  box-shadow: var(--shadow-md);
  position: relative;
  overflow: hidden;
  z-index: 1;
}

.process-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(135deg, rgba(var(--accent-rgb), 0.03), transparent);
  opacity: 0;
  transition: opacity 0.3s ease;
  z-index: -1;
}

.process-card:hover {
  transform: translateY(-8px) scale(1.02);
  box-shadow: 0 15px 35px rgba(var(--accent-rgb), 0.2);
  border-left-color: var(--accent-light);
}

.process-card:hover::before {
  opacity: 1;
}

/* Efecto de "luz" al hacer hover */
.process-card::after {
  content: '';
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: radial-gradient(
    circle at center,
    rgba(var(--accent-rgb), 0.1) 0%,
    transparent 70%
  );
  transform: scale(0);
  opacity: 0;
  transition: transform 0.6s cubic-bezier(0.22, 1, 0.36, 1), opacity 0.4s ease;
  z-index: -1;
}

.process-card:hover::after {
  transform: scale(1);
  opacity: 1;
}

/* Resaltar el número al hacer hover */
.process-card:hover .process-number-wrapper {
  transform: rotate(15deg) scale(1.1);
  background: var(--accent-regular);
}

.process-card:hover .process-number {
  color: white;
}

.process-number-wrapper {
  transition: all 0.4s cubic-bezier(0.22, 1, 0.36, 1);
}
  

  /* Estructura Grid */
.education-grid {
  display: grid;
  gap: 1.5rem;
}

@media (min-width: 768px) {
  .education-grid {
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  }
}

/* Grid Horizontal para Desktop - Vertical para Móvil */
.education-grid {
  display: grid;
  grid-auto-flow: column; /* Flujo horizontal */
  gap: 1.5rem;
  overflow-x: auto; /* Permite scroll horizontal si no cabe */
  padding-bottom: 1rem; /* Espacio para scroll */
  scrollbar-width: thin; /* Firefox */
  scrollbar-color: var(--accent-regular) var(--gray-800); /* Firefox */
}

/* Webkit Scrollbar (Chrome/Safari) */
.education-grid::-webkit-scrollbar {
  height: 6px;
}
.education-grid::-webkit-scrollbar-track {
  background: var(--gray-800);
  border-radius: 3px;
}
.education-grid::-webkit-scrollbar-thumb {
  background: var(--accent-regular);
  border-radius: 3px;
}

/* Estilos de las tarjetas (ahora en línea) */
.education-card {
  background: linear-gradient(145deg, var(--gray-800), var(--gray-850));
  border-radius: 12px;
  padding: 1.5rem;
  min-width: 280px; /* Ancho mínimo para cada tarjeta */
  transition: transform 0.3s ease;
  border: 1px solid var(--gray-700);
  scroll-snap-align: start; /* Alineación para scroll */
}

/* Versión Móvil (una columna) */
@media (max-width: 767px) {
  .education-grid {
    grid-auto-flow: row; /* Flujo vertical */
    overflow-x: visible; /* Elimina scroll horizontal */
    grid-template-columns: 1fr; /* Una sola columna */
  }
  
  .education-card {
    min-width: auto; /* Ancho automático */
    width: 100%; /* Ocupa todo el ancho */
  }
}

/* Resto de estilos se mantienen igual */
.education-card:hover {
  transform: translateY(-5px) scale(1.02);
  box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
}

.education-icon-wrapper {
  background: var(--gray-700);
  width: 50px;
  height: 50px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 1rem;
}

/* ... (el resto de tus estilos existentes para iconos, barras de progreso, etc) ... */

  /* CTA */
  .cta-box {
    background: linear-gradient(135deg, var(--gray-800), var(--gray-850));
    padding: 2.5rem;
    border-radius: 16px;
    margin-top: 3rem;
    text-align: center;
    border: 1px solid var(--gray-700);
    position: relative;
    overflow: hidden;
  }

  .cta-box::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle, rgba(var(--accent-rgb), 0.1) 0%, transparent 70%);
    animation: rotate 20s linear infinite;
    z-index: 0;
  }

  @keyframes rotate {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  .cta-text {
    font-size: var(--text-xl);
    margin-bottom: 1.5rem;
    position: relative;
    z-index: 1;
  }

  .cta-button {
    background: var(--accent-regular);
    color: white;
    padding: 1rem 2.5rem;
    border-radius: 8px;
    font-weight: 600;
    transition: all 0.3s cubic-bezier(0.22, 1, 0.36, 1);
    position: relative;
    z-index: 1;
    border: none;
    cursor: pointer;
    font-size: var(--text-md);
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
  }

  .cta-button:hover {
    background: var(--accent-light);
    transform: translateY(-3px) scale(1.02);
    box-shadow: 0 10px 20px rgba(var(--accent-rgb), 0.2);
  }

  /* Responsive */
  @media (min-width: 50em) {
    .about-section {
      grid-column: 2;
    }

    .content {
      font-size: var(--text-lg);
    }

    .process-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 80em) {
    .process-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }
</style>